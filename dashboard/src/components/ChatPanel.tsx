export function ChatPanel({ connectionStatus, activeRunId }: { connectionStatus: string; activeRunId: string | null }) {
  return <div>Chat Panel Stub (Status: {connectionStatus}, Run: {activeRunId})</div>;
}
